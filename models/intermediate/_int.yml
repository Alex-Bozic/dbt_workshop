version: 2

models:
  - name: int_order_payments
    description: Intermediate calculation to caluculate payed amount per order
    columns: 
      - name: order_id
        description: Natural key of the table
        tests:
        - unique
        - not_null