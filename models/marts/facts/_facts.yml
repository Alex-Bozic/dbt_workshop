version: 2

models:
  - name: fact_orders
    description: Fact orders
    columns:
      - name: cust_sid
        tests:
          - not_null
          - relationships:
              to: ref('dim_customer')
              field: customer_sid